language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t gueuze/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true gueuze/docker-react npm run test -- --coverage
